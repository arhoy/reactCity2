import React from 'react';
import classes from './MatchBlock';

const MatchBlock = (props) => {
    return (
        <div className = {classes.MatchBlock}>

        </div>
    );
};

export default MatchBlock;